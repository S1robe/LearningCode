fn main() {

    
}

// Give me the file system
use std::fs;
//Let me read environment variables

fn problem1(){
    //Basic idea is to sum them and then only store the max
    let file = fs::read_to_string("./assets/1/input.txt").expect("File not found!");
        //Let me read environment variables// some how get file? 
    let mut mx = 0;
    let mut cur = 0;
}
